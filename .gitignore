# --- Runtime / local data (PHP tự tạo nếu thiếu — xem lib/storage.php) ---
/htdocs/data/iot_store.json
/htdocs/data/*.local.json

# --- Secrets & môi trường ---
.env
.env.*
!.env.example

# --- PHP / Composer (nếu sau này thêm) ---
/vendor/
composer.phar

# --- Logs & cache ---
*.log
.phpunit.result.cache
.php-cs-fixer.cache

# --- Windows ---
Thumbs.db
ehthumbs.db
Desktop.ini
$RECYCLE.BIN/

# --- macOS ---
.DS_Store
.AppleDouble
.LSOverride

# --- Editor / IDE ---
.idea/
*.sublime-project
*.sublime-workspace
*.swp
*.swo
*~

# --- Node (nếu sau này thêm tooling front-end) ---
node_modules/

# --- Backup / tạm ---
*.bak
*.tmp
*.temp
